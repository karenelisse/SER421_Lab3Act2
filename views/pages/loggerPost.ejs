<!-- views/pages/logger.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/head %>
</head>
<body class="container">
    <h1>Welcome <%= userName %> you currently have <%= userRoles %> privileges</h1>

<main>
    <div class="jumbotron">
     
        <p><form action="/" method=GET>
        
        <p><input type="submit" value="Log Out"></form></p>
      <% if(userRoles == "Reporter"){%>
        <% include ../partials/linkToAdd %> <br> <br>
        <% for (var i =0; i < title.length; i++){ %>
   
    
    
    <%}}%>
    
     <% for (var i =0; i < title.length; i++){ %>
       <% if(author[i][0] === userName && userRoles == "Reporter" ) {%>      
        <a href='/content/<%=i%>'><%= title[i] %> </a> <br>
             <%}else if(userRoles != "Reporter") {%>
        <a href='/content/<%=i%>'><%= title[i] %> </a> <br>
        <%}%>
        <%} %>
            
    
        </p>
    </div>
</main>

<footer>
    <% include ../partials/footer %>
</footer>

</body>
</html>